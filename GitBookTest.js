import { Button } from '@rneui/base';

const gitbookTest = () => {
  return <Button title="Hello World" />;
}
  


  const styles = StyleSheet.create({
    container: {
      flex: 1,
      flexDirection: 'row', 
      marginTop: "3rem"
    },
    area: {
        
    }
  
  });

export default gitbookTest
